dist: bionic
language: python
python:
    - "3.8"
addons:
    apt:
        update: true
        packages:
            - tree
install:
    - pip install pipenv
    - pipenv install
script:
    - website build
    - tree out/
